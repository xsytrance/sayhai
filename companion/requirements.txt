# --- Phase 0: scaffold + the alive face ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
# Optional fullscreen chrome-less desktop window on the Legion Go (USE_WEBVIEW=true):
# pywebview>=5.0

# --- Phase 1: brain + voice OUT ---
httpx>=0.27            # talk to Ollama
# Text-to-speech (Kokoro). Needs the model files too — see README ("Voice").
kokoro-onnx>=0.4
onnxruntime>=1.17
numpy>=1.24

# --- Phase 2: ears (voice IN) ---
# Speech-to-text. Model (base.en, ~150MB) auto-downloads on first use.
faster-whisper>=1.0

# --- Later phases (roadmap; install when you get there) ---
# Phase 3 channels: python-telegram-bot
# Phase 4 senses:   sounddevice  winsdk
# Phase 6 cloud:    elevenlabs
