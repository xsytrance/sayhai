"""The Mind — Ollama call + personality prompt + JSON emotion parsing.

Stub. Lands in Phase 1. Will send user text to Ollama and parse a
`{"emotion", "text", "action"}` reply, then push the emotion onto the hub so the
face reacts. Nothing here runs in Phase 0.
"""
